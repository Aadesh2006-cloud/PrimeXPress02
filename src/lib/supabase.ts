import { supabase, SUPABASE_PROJECT_ID, SUPABASE_DEFAULT_URL, SUPABASE_DEFAULT_KEY } from '../supabaseClient';

export { supabase, SUPABASE_PROJECT_ID, SUPABASE_DEFAULT_URL, SUPABASE_DEFAULT_KEY };
export default supabase;
